STEP 1: 
project folder name 
cd project folder name 

STEP 2:
check python version : 
python3 --version 

STEP 3:
activate virtual env 
python3 -m venv venv
source venv/bin/activate  for mac : 

STEP 4:
install django
pip install django

install REST frameworks
pip install djangorestframework

STEP 5:
create django project : 
django-admin startproject backend .         it create /backend and the manage.py 


project structure meaning : 

| File        | Purpose               |
| ----------- | --------------------- |
| manage.py   | Django command runner |
| settings.py | Project configuration |
| urls.py     | Routes                |
| wsgi.py     | Deployment            |
| asgi.py     | Async server config   |


STEP 6:
runserver : 
python manage.py runserver



STEP 7 
Create Employees App
python manage.py startapp employees

FOLDER STRUCTURE 
employees/
│
├── admin.py
├── apps.py
├── migrations/
├── models.py
├── tests.py
├── views.py

IMP FILE MEANING : 
| File        | Purpose            |
| ----------- | ------------------ |
| models.py   | Database models    |
| views.py    | API logic          |
| admin.py    | Django admin panel |
| migrations/ | Database changes   |


STEP 8:
register employye app in main project (backend settings.py)
FIND : 
INSTALLED_APPS = []
ADD : 
'rest_framework',
'employees',

STEP 9:
CREATE FIRST EMPLOYEE models
open employee/models.py
Replace Everything With
*******************************************************************
from django.db import models
class Employee(models.Model):

    name = models.CharField(max_length=100)

    email = models.EmailField(unique=True)

    department = models.CharField(max_length=100)

    salary = models.IntegerField()

    is_deleted = models.BooleanField(default=False)

    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return self.name

*******************************************************************
    # __str__ this Controls how object appears in Django admin panel.


STEP 10:
CREATE MIGRATIONS 
Convert model changes into database changes
python manage.py makemigrations

STEP 11:
APPLY MIGRATIONS TO DATABASE : 
python manage.py migrate

what happen : create db.sqlite3 and the employee table inside the database 


STEP 12:
OPEN DJANGO ADMIN PANNEL 
python manage.py creaetsuperuser

it will ask the credential : 
username:
email:
password


STEP 13:
REGISTER MODEL IN ADMIN
inside : employees/admin.py

from django.contrib import admin
from .models import Employee
admin.site.register(Employee)

Without this:
Employee model won't appear in admin panel

STEP 14:
RUN SEVER AGAIN : 

python manage.py runserver

STEP 15:

CREATE THE SERIALIZER.PY FOR converting python boject data into json
inside /employee/serializer.py

and add this code inside : 

from rest_framework import serializers
from .models import Employee


class EmployeeSerializer(serializers.ModelSerializer):

    class Meta:

        model = Employee

        fields = '__all__'

STEP 16: 
CREATE THE FIRST API views


















PRACTICE PRACTICE 

UserMnagement
cd UserMnagement
python3 --version
python3 -m venv venv
source venv/bin/activate
pip install django djangorestframework
django-admin startproject Users-project .
python manage.py runserver
python manage.py startapp users
register in settin.py 
create model in users/model
apply createmigrations
then python manage.py migrate 
create admin pannel using superuser
register model in admin.py of the project 
run server 
add serializer.py in the app folder to convert model data to object json

